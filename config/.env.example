# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (change in production!)
JWT_SECRET=meitzad-secret-key-change-this-in-production

# Admin Email
ADMIN_EMAIL=admin@meitzad.org.il

# WhatsApp Configuration
WHATSAPP_GROUP_ID=your-whatsapp-group-id
WHATSAPP_BRIDGE_URL=http://localhost:3001

# Email SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=pniyot@meitzad.org.il
SMTP_PASS=your-app-password
SMTP_FROM="מערכת פניות מיצד" <pniyot@meitzad.org.il>
